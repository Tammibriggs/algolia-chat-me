---
title: "Bot Customization"
description: "Customize the chatbot persona using setBotPersona(name, tone, description)."
category: "Core Features"
---

# Bot Customization

Personalize your chatbot to match your brand or use case.

## Set Bot Persona

Use `setBotPersona(name, tone, description)` to change the bot's name, conversational style, and description.

```js
sdk.setBotPersona('Ava', 'friendly', 'AI Assistant');
```

- `name`: Display name for the bot (e.g., "Ava", "SupportBot")
- `tone`: Conversational tone (e.g., "friendly", "professional", "playful")
- `description`: A short text describing the bot's role (e.g., "AI Assistant", "Support Specialist").

### Example Use Cases
- Make the bot sound more formal for enterprise support
- Switch to a playful tone for gaming communities

### Edge Cases
- If `tone` is omitted, defaults to "neutral".
- If `description` is omitted, it will be unset.
- Invalid `tone` values will fallback to default.

---

**See also:** [Live Chat Init](./live-chat-init.mdx)